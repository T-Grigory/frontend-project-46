// export default (ast1, ast2) => {
//   const space = 4;
//   const iter = (tree, deep) => {
//     //const [name, type, value] = Object.keys(node);
// 	const indent = deep * space;
// 	if (tree.type === 'node') {
// 		tree.value.map((node) => {
// 			iter(node, deep + 1)
// 		})
// 	}
//     return;
//     // const indent = deep * 4;
//   };
//   return iter(ast2, 1);
// };
